

function sum(a, b) {  // defines a function  sum  that takes two arguments  a  and  b 
    var c = a + b;  // adds  a  and  b  together and assigns the result to  c 
    console.log(c);  // logs the result  c  to the console
}

sum(2, 4);  // calls the  um  function with arguments 2 and 4